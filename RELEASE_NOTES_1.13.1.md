# CodeCarry 1.13.1

- 修复 DSH 预设加载被无关会话请求和其他服务器状态变化拖延的问题，加载失败或连接变化后可重试。
- Codex 会话页对齐原生项目界面，按项目展开会话并提供对应管理操作。
- Codex 会话右滑重命名，左滑归档；归档视图中左滑恢复。
- Codex 新建对话支持完整远端目录浏览、返回上级和选择当前目录。
- Codex 输入框回车改为换行，发送由发送按钮触发。
- Codex 对话显示已发送图片，历史远端图片通过服务端读取；读取失败可重试。

版本 `1.13.1`，版本代码 `121`。Codex 目录浏览需要服务端支持
`fs/readDirectory`，历史远端图片读取需要 `fs/readFile`；不支持或访问失败时显示明确错误。
