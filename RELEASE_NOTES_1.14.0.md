# CodeCarry 1.14.0

- Codex 子代理按父任务分组，支持多层展开、运行/失败/待处理汇总，以及聊天内关联任务导航。
- Codex 错误按线程和轮次展示，保留错误详情与自动重试状态；本地操作错误不再被无关通知覆盖。
- 三种后端均可复制已加载消息，保留 Markdown、代码和折叠工具输出。
- 同一 Markdown 渲染行内的正文共享选择范围，Codex 流式 Markdown 复用已完成段落的规划。

复制范围是当前已加载历史；尚未加载的历史页不包含在导出中。代码、表格和数学区域保留各自交互范围。
