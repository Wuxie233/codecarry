# CodeCarry 1.13.0

## DSH 预设

- 在聊天中查看和主动选择当前会话预设，无需输入会话 ID。
- 预设选择支持搜索、描述、默认标识和损坏状态。
- 新建会话可指定预设，选择工作目录和复用空会话时保留该选择。
- 仅普通空闲会话可切换；服务端确认后更新，失败可重试。

## Codex 手机控制面

- 优化聊天布局、思考和工具折叠，以及阅读历史时的流式跟随行为。
- 审批和提问集中在输入框上方，支持多请求处理和防重复提交。
- 明确新消息、补充当前任务、停止及发送结果待确认状态。
- 支持相册和拍照图片、Skill 选择及远端项目文件引用。
- 展示结构化任务计划、文件改动 diff 和子代理会话入口。
- 会话状态面板集中展示目标、记忆模式和上下文用量。
- 会话列表增加运行中和待处理筛选，新建支持选择最近目录。

## 兼容性

- Codex 继续使用原生 app-server 协议，并与终端共享同一 daemon。
- 可选功能取决于服务器版本；不支持的接口显示错误而不影响基本聊天。
- 记忆控制指会话记忆模式，不包含记忆内容编辑。
- Android 8.0+；版本号 `1.13.0`，版本代码 `120`。
