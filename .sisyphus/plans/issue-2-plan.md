# Issue #2 Session List Follow-ups — Task Graph

> refs #2 (Wuxie233/oc-remote)
> Generated by planning agent
> Scope: Android client only; no cross-agent interface contract required

---

## 任务拆解

### T1 — 去重 grouped session 行内项目标签
**目标**：修掉 grouped UI 下 header 与 row 双重展示目录/项目名，避免中文长文案时重复与视觉重叠。
**文件/模块范围**：
- `app/src/main/java/.../session/SessionListViewModel.kt`
- `app/src/main/java/.../session/SessionListScreen.kt`
- 仅触及 grouped row `dirLabel/projectName` 传递与渲染判定
**QA:** 在已分组的会话列表中准备同一项目下至少 2 个 root session，启动 app 后进入 Session List，人工检查 `ProjectGroupHeader` 仍显示项目/目录信息，但每个 `SessionRow` 不再重复显示同一目录标签；预期结果：标题区无重叠，中文长项目名也只出现一次且列表行高度正常。

---

### T2 — 修复筛选持久化导致的空列表卡死
**目标**：让 filter 只在当前 server / 当前数据集上安全生效，并在 filter 无匹配或新建会话后不会把列表永久卡空。
**文件/模块范围**：
- `app/src/main/java/.../session/SessionListViewModel.kt`
- `app/src/main/java/.../session/SessionListPreferencesRepository.kt`
- `app/src/main/java/.../session/SessionListTopControls.kt`
- 如有需要补充 `SessionListUiState` / filter model
**QA:** 用 debug 包运行 app，在同一 server 下依次点击 `All / Working / Has changes / Has errors / Archived` filter chip，包含一次“选到无匹配 filter 后立刻新建 session 再返回列表”的复现；必要时用 `adb logcat | grep SessionList` 辅助看 state。预期结果：无论当前 filter 是否无匹配，都不会出现永久空列表；切回 `All` 后旧会话立即恢复显示，新建会话在合理 filter 下可见或自动回到可见状态。

---

### T3 — 补齐本次会话列表与 picker 相关 zh-CN 文案
**目标**：补齐会话搜索/排序/过滤、项目分组、子代理、时间元信息等本次改动涉及的 `values-zh-rCN` 缺失 key，确保新旧入口至少有 zh-CN。
**文件/模块范围**：
- `app/src/main/res/values-zh-rCN/strings.xml`
- 必要时对照 `app/src/main/res/values/strings.xml`
- 仅补齐本次需求直接涉及的 key
**QA:** 将系统语言或 app 语言切到 `简体中文`，依次打开 Session List 搜索/排序/过滤区、项目分组菜单、subagent 展开入口、Chat 的 thinking/agent picker；预期结果：本次列出的相关文案全部显示为 zh-CN，无英文 key 名、无缺失占位、无回退到英文的新增入口。

---

### T4 — 抽出共享单选列表 picker 基础能力
**目标**：把模型选择器已有的 `BasicAlertDialog + LazyColumn + selected highlight + trailing checkmark` 模式提炼成可复用单选列表 dialog，供模型/思考强度/agent type 复用，且保持最小重构。
**文件/模块范围**：
- `app/src/main/java/.../chat/ChatScreen.kt`
- `app/src/main/java/.../chat/components/` 下现有 picker/dialog 相关文件（如已有独立组件则优先落那里）
- 避免改动消息发送、草稿持久化协议本身
**QA:** 运行 app 进入 Chat，先打开现有模型选择器，再打开新抽象复用后的任一单选 picker（可临时由 thinking/agent 接入后验证）；人工比对交互骨架。预期结果：弹窗都使用列表式单选 UI，包含当前项高亮、尾部 checkmark、点击后关闭并回填选择，且模型 picker 行为未回归。

---

### T5 — 把 thinking intensity 与 agent type 改成列表式 picker
**目标**：新增 `selectVariant(String?)` 等最小 ViewModel 入口，并把 `ChatInputBar` 中当前“循环切换”的 thinking/agent 交互改成与模型选择一致的列表式 picker。
**文件/模块范围**：
- `app/src/main/java/.../chat/ChatViewModel.kt`
- `app/src/main/java/.../chat/ChatScreen.kt`
- `app/src/main/java/.../chat/components/ChatInputBar.kt`
- `app/src/main/java/.../draft/DraftRepository.kt`（仅在需要核对现有持久化调用时）
**QA:** 在 Chat 页面分别点击 thinking intensity 与 agent type 入口，验证它们都弹出列表式 picker 而不是循环切换；各选一个非默认项后发送消息、退出并重进当前会话。预期结果：两处入口都能直接选中指定项，选中状态在 UI 中立即更新，消息发送正常，重新进入后仍保持已持久化的 variant/agent。

---

### T6 — 集成验证与回归收口
**目标**：把 T1/T2/T3/T5 集成后做一次最小回归，确认 grouped session、filter、zh-CN、两个新 picker 之间没有交叉回归，并补必要的轻量 UI/状态修正。
**文件/模块范围**：
- 以上改动文件的集成收口
- 可能补 1 个 UI state / helper 小修，不额外扩面
- 验证命令以 Android 编译/LSP 为主
**QA:** 先运行 `./gradlew assembleDebug`，并对改动目录执行 `lsp_diagnostics`/IDE diagnostics 确认无新增错误；然后在 Android 端按原始 4 个问题逐项手测：① grouped session 页面确认 header/row 不再重复或重叠；② 切到 zh-CN 后检查新增列表、filter、项目菜单、picker 文案；③ 打开 thinking intensity 与 agent type，确认均为列表式 picker；④ 先点一个会导致当前列表无匹配的 filter，再返回/新建 session/切回 All，确认列表不会一直为空。预期结果：Debug 构建通过，诊断干净，4 个问题均可稳定复现旧路径但在新版本中表现为已修复。

---

## Wave 分组

```text
Wave 1（可并行）
├── T1 去重 grouped session 行内项目标签
├── T2 修复筛选持久化导致的空列表卡死
└── T3 补齐本次会话列表与 picker 相关 zh-CN 文案

Wave 2（依赖 T4 先完成基础抽象；可局部并行）
├── T4 抽出共享单选列表 picker 基础能力
└── T5 把 thinking intensity 与 agent type 改成列表式 picker（依赖 T4；可在 T4 API 定稳后接力）

Wave 3（最终收口）
└── T6 集成验证与回归收口（依赖 T1/T2/T3/T5）
```

### 依赖说明
- **T1/T2/T3** 互不依赖，可同波并行。
- **T4** 与会话列表修复基本解耦，可和 Wave 1 并行启动，但 **T5 必须等待 T4 的共享 picker API 定稳**。
- **T6** 必须在会话列表修复、zh-CN 补齐、picker 替换完成后统一回归。

---

## 每个子任务派发建议

| 任务 | subagent category | 预期 load_skills |
|------|-------------------|------------------|
| T1 去重 grouped session 行内项目标签 | quick | `tester-first-execution` |
| T2 修复筛选持久化导致的空列表卡死 | deep | `tester-first-execution`, `debugging-with-evidence-notes` |
| T3 补齐 zh-CN 文案 | quick | `tester-first-execution` |
| T4 抽出共享单选列表 picker 基础能力 | unspecified-high | `frontend-ui-ux`, `tester-first-execution` |
| T5 改造 thinking / agent 为列表式 picker | visual-engineering | `frontend-ui-ux`, `tester-first-execution` |
| T6 集成验证与回归收口 | oracle | `tester-first-execution`, `debugging-with-evidence-notes` |

---

## 递归派下级建议

- **T2 可再拆 2 个 L2 子任务**：
  1. `SessionListPreferencesRepository` 的 filter 作用域/持久化键修正。
  2. `SessionListViewModel` 的空态兜底、新建会话后 filter 行为、child/subagent 参与逻辑修正。
  仅当实现者想把“存储层改动”和“UI state 改动”并行时再拆；否则单 agent 直接做更稳。

- **T4/T5 可采用“父子接力”而不是完全递归**：
  - T4 先产出共享 picker 组件/API；
  - T5 在同一 L1 或接力 L1 上接入 thinking/agent 两处入口。
  不建议再往下拆到更多 UI 小任务，否则会在 `ChatScreen.kt` / `ChatInputBar.kt` 产生不必要冲突。

- **T1/T3/T6 不建议再拆**：
  - T1 改动面很小，拆分收益低；
  - T3 是单文件文案补齐；
  - T6 本质是收口和验证，拆分会增加协调成本。

---

## 风险点 / 阻塞

1. **filter 修复真实边界不止一个点**：如果全局持久化、root session 过滤、空态判断、新建会话后的默认可见性这几处没有一起收口，问题会从“永久空列表”退化成“偶发空列表”而非真正修复。

2. **picker 抽象容易过度设计**：T4 若演变成全量重构所有 dialog，会扩大 diff 并拖慢 T5；应只抽“单选列表对话框”最小公共骨架，不动无关行为。

3. **`ChatScreen.kt` / `ChatInputBar.kt` 与 `SessionListScreen.kt` 都是热点文件**：虽然会话修复和 picker 修复可并行，但各自内部最好单线落地，避免多人同时改同一 UI 文件导致冲突回滚。

---

## 不在范围内（YAGNI）

- 不做其他 locale 的全量翻译，只保证 `zh-CN` 与英文主 key 对齐到本次需求范围。
- 不改服务端、OpenCode 协议、session API 返回结构。
- 不顺手重做整个 session list 分组架构，只修本次 4 个已确认问题所需最小面。
- 不把所有 chat picker 统一成一个大型通用框架，只抽本次 thinking/agent 复用所需的最小单选列表能力。
- 不新增 release 流程、截图产物流程、额外 QA 平台接入。

---

## 推荐执行顺序

1. 先并行完成 **T1/T2/T3/T4**。
2. `T4` 产出稳定 API 后立刻执行 **T5**。
3. 最后由 **T6** 做统一收口，重点验证：
   - grouped session 不再重复显示目录/项目名；
   - 切换各 filter 后不会永久空列表；
   - 新建 session 后列表可见；
   - zh-CN 下搜索/排序/过滤/picker 文案完整；
   - thinking intensity / agent type 都是列表式 picker，交互与模型 picker 一致。
