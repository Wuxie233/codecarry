# CodeCarry 1.14.10

修复 Codex 与 DSH 的会话恢复问题（#48、#49）：

- Codex：修复已读游标已是最新、未读标记仍无法清除的问题；迟到的未读标记可以在查看最新回复时自动清除。
- Codex：归档、移出归档及线程关闭后重新验证订阅，避免旧缓存继续显示为可用。
- Codex：取消订阅采用独立的 10 秒期限，超时后重新建立连接，避免后台清理占用一般请求的 120 秒等待时间。
- DSH：重连期间保留已显示的聊天内容，收到历史快照后再更新；会话目录加载失败不再阻断已知对话恢复。
- DSH：先注册订阅接收器，再发送订阅请求，修复快速返回的首帧历史快照丢失。

## Verification

- 修复提交通过 996 项 JVM 测试，Debug APK 构建成功。
- 手机端归档恢复、重连体验及现场网络耗时留待本版本验证。

## Metadata

- `versionName`: `1.14.10`
- `versionCode`: `134`
