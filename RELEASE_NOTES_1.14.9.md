# CodeCarry 1.14.9

修复 2026-09 审查批次全部 18 个问题(#30–#47),重点:

- 会话状态、消息与部件按服务器隔离:两台服务器出现相同会话/消息 ID 时不再互相覆盖,断线清理不再误删另一台的数据。
- 全新跨后端未读链路(OpenCode / DSH / Codex):离开聊天后完成的新回复持久标记未读,真正读到回复才清除,重启保留;进入聊天但未看到新回复不再误清未读;后台聊天不再抑制提醒。
- Codex:发送确认读回不再让页面回退到旧快照;目标与模型目录加载失败可独立重试并区分状态;记忆模式显示明确的"本设备已设置"回执;MCP 必填布尔表单可直接提交显示的 false;会话面板展示当前审批策略与沙箱边界。
- 通用会话列表:有内容时操作失败可见可重试;断线与操作进行中禁用相应操作并防连点重入;项目归档使用当前服务器权威元数据。
- DSH 扩展控制面:各模块独立加载与重试,单项失败不再拖垮整页;新增技能加载入口与会话上下文选择器;preset/goal/subagent 操作继承会话上下文,设置表单化,裸 JSON 移入显式高级入口。

## Verification

- `:app:testDebugUnitTest` 987 项测试通过;`:app:assembleDebug` 通过。
- Emulator、设备与真实会话 E2E 未在本轮执行。

## Metadata

- `versionName`: `1.14.9`
- `versionCode`: `133`
