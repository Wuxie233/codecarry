# CodeCarry 1.14.6

- Codex 聊天输入区新增 Fast 开关，按模型支持情况启用；运行中切换在下一轮生效，保留当前模型与推理强度。
- 新增账户额度入口，显示各额度池的剩余比例、周期、重置时间及服务端提供的 Credits。
- 额度支持实时推送和手动刷新；连接恢复、聊天回到前台时自动更新，断线时明确标记旧数据。
- Fast 未发送的选择按服务器和会话保存，发送成功后以服务端恢复状态为准；首次开启提示额外消耗。
