# CodeCarry 1.14.12

- Codex 可在编辑现有草稿时，通过光标附近的 `/` 查询选择技能，不再限于整个输入框的开头。
- 技能名支持模糊匹配：输入 `/tospec` 可找到 `to-spec`；精确名称和前缀匹配优先，仍支持描述搜索。
- 选中技能只移除当前查询，保留前后正文、换行和正确光标位置；技能仍作为附件，由发送按钮提交。
- 保留中文及输入法组合输入；移动光标后旧候选不会误改新草稿，选区、URL 和多段路径不会被当成技能调用。

## Verification

- 1008 项 JVM 测试（含 Compose 光标与技能选择交互）及 Debug APK 构建通过。
- 未进行物理手机与实际输入法验收。

## Metadata

- `versionName`: `1.14.12`
- `versionCode`: `136`
